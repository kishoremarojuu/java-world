search:
find -iname "filename"
-----------------------------------------------------------------------------------------------

copy all files from one directory to another directory
cp /tmp/putmember/*.* ./putmember

cp -r /tmp/putmember/*.* ./putmember
-----------------------------------------------------------------------------------------------
remove the directory and its contents
rm -R -f directorname


----------------------------------------------------------------------------------------
copy the directory

cp -R source/ destination/
----------------------------------------------------------------------------------------
to view the last 300 lines
 tail -300 success_inbound-20170330-1143.dat

----------------------------------------------------------------------------------------
create alias down below and type only 'l' instead of 'ls -lrt' everytime
alias l="ls -al"
-----------------------------------------------------------------
to delete the line
escape dd
-----------------------------------------------------------------
how to copy the file from one server to another server
scp shivak@10.11.67.15:/tmp/empistprprovput.war shivak@172.18.163.115:/tmp

To copy a file from B to A while logged into B:

scp /path/to/file username@a:/path/to/destination
-----------------------------------------------------------------
ec2 important paths:
cd /etc
cd /var/log/cloud-init-output.log
cd /prod